<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>Insert title here</title>
		<script type="text/javascript">
			function send(f){
				var name = f.m_name.value.trim(); //폼 태그안에 존재하는 m_name이라는 name을 가진 input의 value를 가져옴.
				
				//유효성 체크
				if(name == ""){
					alert("이름을 입력하세요!!");
					return;
				}
				
				f.submit();	//폼태그의 정보를 넘겨준다.
			}
			
		</script>
	</head>
	<body>
		<!-- 서버로 데이터를 전송하고자 한다면 form태그가 반드시 필요하다! -->
		<form action="hello.serv"> <!-- action : form태그의 name정보를 전송하고자 하는 서버쪽 객체의 url -->
			이름 : <input name = "m_name"><br>
			<input type = "radio" name = "nation" value = "kor" checked = "checked">한국<br>
			<input type = "radio" name = "nation" value = "eng">미국<br>
			<input type = "radio" name = "nation" value = "jpn">일본<br>
			<input type = "radio" name = "nation" value = "chn">중국<br>
			<input type = "button" value = "알려줘!" onclick = "send(this.form);">
			<!-- <input type = "submit" value = "전송"><br>  
			//이렇게 만들면 값이 없어도 전송된다. 그러나 파라미터의 유효성 체크를 하지않고 바로 서버로 넘어가므로 비추. -->
		</form>
		<!-- 
		http://localhost:9090/Ex_0514_Servlet/hello.serv?m_name=재휘&nation=eng
		안에 들어가있는 value값이 parameter로 서버에 넘어가는 것을 확인할 수 있다.
		-->
	</body>
</html>